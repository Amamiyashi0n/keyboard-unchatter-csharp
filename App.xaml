<Application x:Class="keyboard_unchatter_csharp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:keyboard_unchatter_csharp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="PrimaryBrush" Color="#FF6200EE"/>
            <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#FF3700B3"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#FF03DAC6"/>
            <SolidColorBrush x:Key="SurfaceBrush" Color="#FFF5F5F5"/>
            <SolidColorBrush x:Key="CardBrush" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FF212121"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FF757575"/>
            <SolidColorBrush x:Key="ActiveBrush" Color="#FFFF6F00"/>
            <SolidColorBrush x:Key="InactiveBrush" Color="#FF9E9E9E"/>
            <SolidColorBrush x:Key="NavBackgroundBrush" Color="#FFFFFFFF"/>

            <Style TargetType="{x:Type Window}">
                <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            </Style>

            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Style>

            <Style x:Key="TitleText" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <Style x:Key="SubtitleText" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="#DDFFFFFF"/>
            </Style>

            <Style x:Key="SectionTitle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="NavGroupTitleText" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="Margin" Value="0,2,0,10"/>
            </Style>

            <Style x:Key="NavToggleButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="MinWidth" Value="48"/>
                <Setter Property="MinHeight" Value="48"/>
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="24" Padding="{TemplateBinding Padding}">
                                <Border.Effect>
                                    <DropShadowEffect x:Name="ButtonShadow" BlurRadius="8" ShadowDepth="2" Opacity="0" Color="#000000"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="BlurRadius">
                                                    <SplineDoubleKeyFrame Value="16" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="ShadowDepth">
                                                    <SplineDoubleKeyFrame Value="4" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="Opacity">
                                                    <SplineDoubleKeyFrame Value="0.3" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="BlurRadius">
                                                    <SplineDoubleKeyFrame Value="8" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="ShadowDepth">
                                                    <SplineDoubleKeyFrame Value="2" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="Opacity">
                                                    <SplineDoubleKeyFrame Value="0" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                    <Setter Property="Background" Value="#22FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="NavListBox" TargetType="{x:Type ListBox}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
            </Style>

            <Style x:Key="NavListBoxItem" TargetType="{x:Type ListBoxItem}">
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="Margin" Value="0,4,0,4"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="12" Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border x:Name="ItemIndicator" Grid.Column="0" Background="Transparent" CornerRadius="2" Width="3" HorizontalAlignment="Center"/>
                                    <ContentPresenter Grid.Column="2" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FFF4F4F4"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FFEFEFEF"/>
                                    <Setter TargetName="ItemIndicator" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="NavCompactListBoxItem" TargetType="{x:Type ListBoxItem}">
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="Margin" Value="0,2,0,2"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="12" Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border x:Name="ItemIndicator" Grid.Column="0" Background="Transparent" CornerRadius="2"/>
                                    <ContentPresenter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FFF4F4F4"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FFEFEFEF"/>
                                    <Setter TargetName="ItemIndicator" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="Card" TargetType="{x:Type Border}">
                <Setter Property="Background" Value="{StaticResource CardBrush}"/>
                <Setter Property="CornerRadius" Value="16"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.12"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="18,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="MinHeight" Value="42"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{StaticResource InactiveBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="OutlinedButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="18,7"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type CheckBox}">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Grid SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Canvas x:Name="CheckBoxCanvas" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                    <Canvas.RenderTransform>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Canvas.RenderTransform>
                                    <!-- Unchecked state -->
                                    <Border x:Name="Box"
                                            Canvas.Left="1"
                                            Canvas.Top="1"
                                            Width="18"
                                            Height="18"
                                            CornerRadius="2"
                                            Background="Transparent"
                                            BorderBrush="{StaticResource PrimaryBrush}"
                                            BorderThickness="2"/>
                                    <!-- Checked state checkmark -->
                                    <Path x:Name="CheckMark"
                                          Data="M 4 10 L 8 14 L 16 5"
                                          Stroke="White"
                                          StrokeThickness="2.5"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Visibility="Collapsed"
                                          Canvas.Left="0"
                                          Canvas.Top="0"/>
                                </Canvas>
                                <ContentPresenter Grid.Column="2"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimationUsingKeyFrames Storyboard.TargetName="Box" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)">
                                                    <SplineColorKeyFrame Value="#FF6200EE" KeyTime="0:0:0.2" KeySpline="0.2,0,0.2,1"/>
                                                </ColorAnimationUsingKeyFrames>
                                                <ColorAnimationUsingKeyFrames Storyboard.TargetName="Box" Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)">
                                                    <SplineColorKeyFrame Value="#FF6200EE" KeyTime="0:0:0.2" KeySpline="0.2,0,0.2,1"/>
                                                </ColorAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckBoxCanvas" Storyboard.TargetProperty="(Canvas.RenderTransform).(ScaleTransform.ScaleX)">
                                                    <SplineDoubleKeyFrame Value="0.85" KeyTime="0:0:0.05" KeySpline="0.2,0,0.2,1"/>
                                                    <SplineDoubleKeyFrame Value="1.1" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                    <SplineDoubleKeyFrame Value="1" KeyTime="0:0:0.2" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckBoxCanvas" Storyboard.TargetProperty="(Canvas.RenderTransform).(ScaleTransform.ScaleY)">
                                                    <SplineDoubleKeyFrame Value="0.85" KeyTime="0:0:0.05" KeySpline="0.2,0,0.2,1"/>
                                                    <SplineDoubleKeyFrame Value="1.1" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                    <SplineDoubleKeyFrame Value="1" KeyTime="0:0:0.2" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Setter TargetName="Box" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckBoxCanvas" Storyboard.TargetProperty="(Canvas.RenderTransform).(ScaleTransform.ScaleX)">
                                                    <SplineDoubleKeyFrame Value="1.15" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckBoxCanvas" Storyboard.TargetProperty="(Canvas.RenderTransform).(ScaleTransform.ScaleY)">
                                                    <SplineDoubleKeyFrame Value="1.15" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckBoxCanvas" Storyboard.TargetProperty="(Canvas.RenderTransform).(ScaleTransform.ScaleX)">
                                                    <SplineDoubleKeyFrame Value="1" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckBoxCanvas" Storyboard.TargetProperty="(Canvas.RenderTransform).(ScaleTransform.ScaleY)">
                                                    <SplineDoubleKeyFrame Value="1" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                        <Condition Property="IsChecked" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="Box" Property="Background" Value="#0C6200EE"/>
                                </MultiTrigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Box" Property="Opacity" Value="0.55"/>
                                    <Setter Property="Opacity" Value="0.7"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type Slider}">
                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="MinHeight" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Padding" Value="12,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Slider}">
                            <Grid Height="48" Background="Transparent" SnapsToDevicePixels="True" Margin="{TemplateBinding Padding}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Track x:Name="PART_Track" Grid.Row="0" VerticalAlignment="Center" IsHitTestVisible="True">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton x:Name="PART_DecreaseButton" Command="Slider.DecreaseLarge" IsTabStop="False" Focusable="False">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="{x:Type RepeatButton}">
                                                    <Border Background="Transparent" VerticalAlignment="Center" Height="4">
                                                        <Border Background="{StaticResource PrimaryBrush}" CornerRadius="2" Height="4" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton x:Name="PART_IncreaseButton" Command="Slider.IncreaseLarge" IsTabStop="False" Focusable="False">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="{x:Type RepeatButton}">
                                                    <Border Background="Transparent" VerticalAlignment="Center" Height="4">
                                                        <Border Background="#FFE0E0E0" CornerRadius="2" Height="4" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb" Width="24" Height="24" IsTabStop="False" Focusable="False" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                            <Thumb.RenderTransform>
                                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                                            </Thumb.RenderTransform>
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                    <Canvas Background="Transparent" Width="24" Height="24" SnapsToDevicePixels="True">
                                                        <!-- Main circle -->
                                                        <Ellipse Canvas.Left="3" Canvas.Top="3" Width="18" Height="18" Fill="{StaticResource PrimaryBrush}"/>
                                                        <!-- Highlight -->
                                                        <Ellipse Canvas.Left="3" Canvas.Top="3" Width="18" Height="18" Stroke="#33FFFFFF" StrokeThickness="1.5"/>
                                                    </Canvas>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                            <Thumb.Effect>
                                                <DropShadowEffect x:Name="ThumbShadow" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                            </Thumb.Effect>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.RenderTransform).(ScaleTransform.ScaleX)">
                                                    <SplineDoubleKeyFrame Value="1.25" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.RenderTransform).(ScaleTransform.ScaleY)">
                                                    <SplineDoubleKeyFrame Value="1.25" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.Effect).(DropShadowEffect.BlurRadius)">
                                                    <SplineDoubleKeyFrame Value="12" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.Effect).(DropShadowEffect.Opacity)">
                                                    <SplineDoubleKeyFrame Value="0.35" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.RenderTransform).(ScaleTransform.ScaleX)">
                                                    <SplineDoubleKeyFrame Value="1" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.RenderTransform).(ScaleTransform.ScaleY)">
                                                    <SplineDoubleKeyFrame Value="1" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.Effect).(DropShadowEffect.BlurRadius)">
                                                    <SplineDoubleKeyFrame Value="4" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(Thumb.Effect).(DropShadowEffect.Opacity)">
                                                    <SplineDoubleKeyFrame Value="0.2" KeyTime="0:0:0.15" KeySpline="0.2,0,0.2,1"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="PART_DecreaseButton" Property="Opacity" Value="0.45"/>
                                    <Setter TargetName="PART_IncreaseButton" Property="Opacity" Value="0.45"/>
                                    <Setter TargetName="Thumb" Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="OutlinedTextBox" TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#FFDADADA"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Border" Property="Opacity" Value="0.55"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ListView}">
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>

            <Style TargetType="{x:Type GridViewColumnHeader}">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Background" Value="#FFF1F1F1"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
